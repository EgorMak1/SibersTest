@page
@model SibersTestTask.Pages.Index1Model

@{
    ViewData["Title"] = "EmployeesInProject";
}

<h1>EmployeesInProject</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.EmployeesInProject[0].Id)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.EmployeesInProject[0].IdEmployee)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.EmployeesInProject[0].IdProject)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model.EmployeesInProject)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.Id)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.IdEmployee)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.IdProject)
                </td>
                <td>
                    <a asp-page="./Edit" asp-route-id="@item.Id">Edit</a> |
                    <a asp-page="./Details" asp-route-id="@item.Id">Details</a> |
                    <a asp-page="./Delete" asp-route-id="@item.Id">Delete</a>
                </td>
            </tr>
        }
    </tbody>
</table>